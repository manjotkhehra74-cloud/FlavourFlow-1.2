# HRMate Flutter app

Application ID: `com.gdfoods.hrmate`

Flutter SDK is intentionally not installed in the remote server environment. Run locally or in CI:

```bash
flutter pub get
flutter analyze
flutter test
```
